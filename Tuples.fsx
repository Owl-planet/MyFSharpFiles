open System
// Tuple of two integers.
( 4, 5 )

// Triple of strings.
( "one", "two", "three" )

let a = 10
let b = 20
// Tuple of unknown types.
( a, b )

// Tuple that has mixed types.
let ff = ( "Absolute Classes", 1, 2.0 )
Console.Write(ff)
// Tuple of integer expressions.
( a * 4, b + 7)